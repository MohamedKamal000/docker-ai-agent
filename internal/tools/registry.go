package tools

// write an interface for the tools, an some source of a place where it holds all tools
// we need a tool adapter as well, that takes the tool required to be executed and its input
// then notify or return a response somehow that will later be notifed to the user either in the TUI or in the web

// tools will maily be some kind of a wrapper around the docker sdk client that is also need to be done
